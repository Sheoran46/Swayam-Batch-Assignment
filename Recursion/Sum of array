// User function Template for Java

class Solution {
    int arraySum(int arr[]) {
        // code here
        return helper(arr, 0, 0);
    }
    public int helper(int[] arr, int i, int sum){
        if(i>=arr.length) return sum;
        return helper(arr, i+1, sum+arr[i]);
    }
}
